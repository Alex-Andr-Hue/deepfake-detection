# Результаты

> **Этот файл генерируется автоматически.** Таблица ниже появится после прогона
> экспериментов — цифры пишутся не руками, а скриптом из
> `results/model_comparison.csv`, поэтому отчёт не может разойтись с тем, что
> реально посчитали.
>
> ```bash
> python scripts/prepare_data.py --all
> python scripts/train.py --all
> python scripts/report.py     # перезапишет этот файл
> ```

## Протокол

Все эксперименты проходят через `run_experiment()` и подчиняются одним правилам:

| Правило | Зачем |
|---|---|
| `set_seed(42)` перед каждым запуском | инициализация и порядок батчей не влияют на сравнение |
| Свежая модель из `build_model()` | результат конфигурации не смешивается с предыдущим обучением |
| Детерминированный hold-out + `split_generator()` | все модели видят одни и те же val и test |
| Одинаковые `epochs`, оптимизатор и расписание lr | бюджет обучения не даёт никому форы |
| Чекпоинт и порог — по val, метрики — по test | валидация не подменяет тестовую выборку |
| ROC-AUC по вероятностям | AUC от жёстких меток — это балансированная точность |

## Сравнение моделей

_Таблица появится после запуска `scripts/report.py`._

## Что показывают сравнения

Каждый вопрос — это пара экспериментов, отличающихся ровно одним фактором.
Скрипт отчёта считает разницу в F1 автоматически.

### Вклад очистки от шума

`InceptionV1` против `InceptionV1 (шумные)`. Шумная и очищенная версии датасета
построены из одного и того же разбиения и содержат одни и те же изображения —
отличается только предобработка, поэтому разница относится на её счёт целиком.

### Вклад частотной ветви

`InceptionV1+Freq` против `InceptionV1`. Модели отличаются только тем, что во
второй на вход дополнительно подаётся высокочастотный остаток
`x - среднее по окну`. Ноутбук `notebooks/01_eda.ipynb` показывает разностную
карту средних спектров real и fake — есть ли этот сигнал в данных вообще.

### Цена обучения с нуля

`ResNet18 (ImageNet)` против `ResNet18`. Одна и та же архитектура и один и тот
же бюджет обучения; отличается только стартовая инициализация.

### Шум как предобучение

`InceptionV1 (шум → чистые)` против `InceptionV1`. Гипотеза: обучение на шумных
данных заставляет модель искать признак, устойчивый к высокочастотным помехам.
Утечки нет — hold-out обеих версий состоит из одних и тех же id, поэтому первая
стадия не видит тестовых изображений второй.

## Как читать метрики

**F1 — основная.** Классы несбалансированы (83% реальных лиц), и accuracy вводит
в заблуждение: модель, всегда отвечающая «real», получила бы около 0.83 без
всякой пользы.

**ROC-AUC** не зависит от выбранного порога и показывает качество ранжирования
отдельно от качества калибровки.

**Порог** подбирается по валидации максимизацией F1. При дисбалансе 5:1 оптимум
обычно заметно ниже 0.5, и `argmax` систематически недобирает recall.
